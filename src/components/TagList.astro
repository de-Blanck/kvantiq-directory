---
interface Props {
  tags: string[];
}
const { tags } = Astro.props;
---
<ul class="flex flex-wrap gap-1.5">
  {tags.map((tag) => (
    <li class="rounded-full bg-info/10 px-2.5 py-0.5 text-xs font-medium text-info">
      {tag}
    </li>
  ))}
</ul>
