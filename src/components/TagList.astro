---
interface Props {
  tags: string[];
}
const { tags } = Astro.props;
---
<ul class="flex flex-wrap gap-1.5">
  {tags.map((tag) => (
    <li class="rounded-md border border-border bg-base px-1.5 py-0.5 font-mono text-[10px] text-text-muted">
      {tag}
    </li>
  ))}
</ul>
