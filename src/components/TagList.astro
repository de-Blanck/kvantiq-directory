---
interface Props {
  tags: string[];
}
const { tags } = Astro.props;
---
<ul class="flex flex-wrap gap-1.5">
  {tags.map((tag) => (
    <li class="eyebrow rounded-md border border-border bg-base px-1.5 py-0.5 text-text-muted">
      {tag}
    </li>
  ))}
</ul>
