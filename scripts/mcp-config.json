{
  "mcpServers": {
    "kvantiq-tools": {
      "command": "npx",
      "args": ["tsx", "scripts/tools/mcp-server.ts"],
      "env": {
        "RESEND_API_KEY": "${RESEND_API_KEY}",
        "CLICKUP_API_TOKEN": "${CLICKUP_API_TOKEN}",
        "CLICKUP_LIST_ID": "${CLICKUP_LIST_ID}"
      }
    }
  }
}
