# Build output
dist/
.astro/

# Dependencies
node_modules/

# Logs
*-debug.log*
scheduled-run-launchd.log

# Environment variables
.env
.env.production

# OS / editor
.DS_Store
.idea/

# Deployment / local tooling
.vercel
.worktrees/

# AI-assistant tool output
.claude/
.ui-swarm/
.visual-signoff/
.continue-here.md

# Generated data (regenerated by `npm run prebuild` from src/content on every build)
data/generated/*
# ...except the sweep history, which scripts/ai-sweep.mjs appends to and no build
# can reconstruct. It is the one file here that is a source, not an artifact.
!data/generated/sweep-runs.json
data/pending-*.json
data/kvantiq.db
data/kvantiq.db-shm
data/kvantiq.db-wal
data/weekly-agent-prompt.md
.env*.local

# Local visual-audit artifacts (Playwright PNGs, preview HTML) — never committed
.local-design-audit/
