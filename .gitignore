# Build output
dist/
.astro/

# Dependencies
node_modules/

# Logs
*-debug.log*
scheduled-run-launchd.log
auto-merge-launchd.log

# The automation kill switch is local and deliberate: pausing automation on this
# machine should not be a commit, and should not propagate to anyone else.
.automation-paused

# Environment variables
.env
.env.production

# OS / editor
.DS_Store
.idea/

# Deployment / local tooling
.vercel
.worktrees/

# AI-assistant tool output
.claude/
.ui-swarm/
.visual-signoff/
.continue-here.md

# data/generated/ holds one file, and it is a source rather than an artifact:
# sweep-runs.json is appended by scripts/ai-sweep.mjs and no build can reconstruct
# it. Anything else appearing here is a leftover.
data/generated/*
!data/generated/sweep-runs.json
data/pending-*.json
data/weekly-agent-prompt.md
.env*.local

# Local visual-audit artifacts (Playwright PNGs, preview HTML) — never committed
.local-design-audit/
